import 'package:flutter/material.dart';

class SmoothShift extends StatelessWidget {
  final Widget child;

  const SmoothShift({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    // TODO
    return child;
  }
}
